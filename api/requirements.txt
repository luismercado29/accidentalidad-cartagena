fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 rompe con bcrypt >= 4.1: al detectar el backend le pasa una
# cadena de prueba de mas de 72 bytes y bcrypt moderno lanza ValueError en vez
# de truncarla, dejando inutilizables el registro y el login.
bcrypt==4.0.1
pydantic==2.5.0
pydantic-settings==2.1.0
openpyxl==3.1.2
pandas==2.1.3
python-multipart==0.0.6
geopy==2.4.0
shapely==2.0.2

# NO agregar scikit-learn ni torch aqui.
#
# Una funcion serverless de Vercel no puede pasar de 250 MB descomprimidos.
# Con scikit-learn el backend pesaba 369 MB y el despliegue no cabia:
# scikit-learn arrastra scipy, que solo entre ambos suman ~168 MB.
# Sin ellos queda en ~190 MB y despliega.
#
# backend/main.py importa scikit-learn, torch, numpy y shapely dentro de
# try/except, asi que su ausencia no rompe el arranque. Los dos endpoints que
# los necesitan responden con un mensaje explicito:
#   - clustering de accidentes
#   - recalculo de puntos negros
# Para tenerlos hay que instalar backend/requirements.txt, que si los incluye,
# en un host sin ese limite de tamano.
#
# graphviz se quito por no usarse en ninguna parte del backend.
